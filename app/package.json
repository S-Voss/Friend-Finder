{
  "name": "friend_finder",
  "version": "1.0.0",
  "description": "A compatibility-based 'friend finder' that takes in results from your user's surverys, and then compares their answers with those from other users.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Stephen Voss",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.4",
    "path": "^0.12.7"
  }
}
